const sum = (first, second) => {
    return first + second;
}

const division = (first, second) => {
    return first / second;
}

const multiply = (first, second) => {
    return first * second;
}

export { sum, division, multiply }